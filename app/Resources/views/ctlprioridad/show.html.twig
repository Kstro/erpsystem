{% extends 'base.html.twig' %}

{% block body %}
    <h1>CtlPrioridad</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ctlPrioridad.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ ctlPrioridad.nombre }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{% if ctlPrioridad.estado %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_priority_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_priority_edit', { 'id': ctlPrioridad.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

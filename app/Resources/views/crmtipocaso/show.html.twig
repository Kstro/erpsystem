{% extends 'base.html.twig' %}

{% block body %}
    <h1>Crmtipocaso</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ crmTipoCaso.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ crmTipoCaso.nombre }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{% if crmTipoCaso.estado %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('crmtipocaso_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('crmtipocaso_edit', { 'id': crmTipoCaso.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

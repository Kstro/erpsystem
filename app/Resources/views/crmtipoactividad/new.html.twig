{#{% extends 'base.html.twig' %}

{% block body %}
    <h1>CrmTipoActividad creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_activities-types_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
#}

 <div class="col-md-12 col-xs-12">
            <div id="pnAdd" class="panel panel-default" style="display:none;">
                    <div class="panel-heading"><div class="hidden pnHeadingLabelAdd">{{pnHeadingLabelAdd}}</div><div class="hidden pnHeadingLabelEdit">{{pnHeadingLabelEdit}}</div></div>
                    <div class="panel-body">
                        <div class="col-md-4">
                            <label>{{nameTipoActividad}}</label>
                            <label class="hidden requiredFields">{{requiredFields}}</label>
                            <input id="txtId" type="hidden" name="txtId" class="input-sm form-control">
                            <input id="txtName" type="text" name="txtName" class="input-sm form-control validateInput">
                        </div>
                        <div class="col-md-4">
                            <label>{{iconTipoActividad}}</label>
                            <div class="input-group iconpicker-container">
                                <input id="txtIcon" type="text" name="txtIcon" class="input-sm form-control hidden validateInput" readonly>
                                <span class="input-group-addon" style="width:20px; height: 30px; border: 1px solid #DDD; border-radius: 5px;"><i class="fa"></i></span>
                            </div>
                        </div>
                        <div class="clearfix"></div>
                        <div class="col-md-4 pull-right text-right" style="margin-top:20px;">
                            <button id="btnCancel" class="btn btn-sm btn-default">{{cancel}}</button>
                            <button id="btnSave" class="btn btn-sm btn-primary">{{save}}</button>
                        </div>
                    </div>
                    {# <div class="panel-footer">Panel Footer</div> #}
            </div>
</div>

{% extends 'base.html.twig' %}

{% block body %}
    <h1>CrmTipoCampania</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ crmTipoCampanium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ crmTipoCampanium.nombre }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{% if crmTipoCampanium.estado %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_crmtipocampania_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_crmtipocampania_edit', { 'id': crmTipoCampanium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

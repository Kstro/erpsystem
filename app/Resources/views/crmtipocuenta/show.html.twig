{% extends 'base.html.twig' %}

{% block body %}
    <h1>CrmTipoCuenta</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ crmTipoCuentum.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ crmTipoCuentum.nombre }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{% if crmTipoCuentum.estado %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_tipocuenta_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_tipocuenta_edit', { 'id': crmTipoCuentum.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

{% extends 'base.html.twig' %}

{% block body %}
    <h1>CtlIndustria</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ctlIndustrium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ ctlIndustrium.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_ctlindustria_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_ctlindustria_edit', { 'id': ctlIndustrium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

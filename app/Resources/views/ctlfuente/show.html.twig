{% extends 'base.html.twig' %}

{% block body %}
    <h1>CtlFuente</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ctlFuente.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ ctlFuente.nombre }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{% if ctlFuente.estado %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_origin-source_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_origin-source_edit', { 'id': ctlFuente.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
